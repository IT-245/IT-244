# Submission Checklist

- Run `schema.sql` in MySQL.
- Run `seed_data.sql`.
- Run `queries.sql`.
- Capture screenshots of SQL code and query output.
- Add the live report link in `PROJECT_REPORT.md`.
- Add the GitHub repository link in `PROJECT_REPORT.md`.
- Export the final report as PDF if required.
